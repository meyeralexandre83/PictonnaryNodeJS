<% var title = 'Accueil' %>
<% include head %>
	<% if(user != '') { %>

		<div class = "container">
		      <div id="listeDessin">
		<% for(var i=0; i<draw.length; i++) { %>
			<li><a href="/guess?drawid=<%= draw[i].id %>">Dessin <%= i %></a></li>


	</div>
	</div>

	<% }} %>
</body>
<html>
